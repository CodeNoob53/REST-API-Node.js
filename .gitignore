node_modules
# Keep environment variables out of version control
.env

/generated/prisma
*.db
*.db-journal
*.log

# Temporary upload storage — keep the folder, ignore its contents
uploads/*
!uploads/.gitkeep
